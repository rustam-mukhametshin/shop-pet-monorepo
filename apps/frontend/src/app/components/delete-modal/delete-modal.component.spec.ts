import {DeleteModalComponent} from './delete-modal.component';

describe('DeleteModalComponent', () => {
  let component: DeleteModalComponent;

  beforeEach(async () => {
    component = new DeleteModalComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
